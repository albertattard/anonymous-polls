micronaut:
  application:
    name: anonymous-polls
  io:
    watch:
      paths: src/main
      restart: true
  router:
    static-resources:
      default:
        enabled: true
        mapping: /**
        paths:
          - classpath:web
